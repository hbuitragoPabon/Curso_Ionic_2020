import { CalificationPipe } from './calification.pipe';

describe('CalificationPipe', () => {
  it('create an instance', () => {
    const pipe = new CalificationPipe();
    expect(pipe).toBeTruthy();
  });
});
